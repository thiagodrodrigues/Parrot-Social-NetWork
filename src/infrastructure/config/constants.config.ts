export default {
    USER: {
        MESSAGES: {
            ERROR: {
                USER_NOT_EXISTS: `Usuário {USER_ID} não existe`,
                USER_ALREADY_EXISTS: `Usuário {USER_ID} já existe existe`,
                USER_UNAUTHENTICATED_USER: `Usuario nao autenticado.`
            }
        }
    },
    POST: {
        MESSAGES: {
            ERROR: {
                POST_NOT_EXISTS: `Postagem {POST_ID} não existe`
            }
        }
    }
}